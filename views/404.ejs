<%- include("layout-top") %>

<section class="pt-16">
  <div class="rounded-3xl border border-black/10 bg-white/70 p-8 shadow-soft dark:border-white/10 dark:bg-white/5 <%= lang==='ar' ? 'text-right' : '' %>">
    <h1 class="text-2xl font-semibold text-slate-900 dark:text-white"><%= lang==='ar' ? 'الصفحة غير موجودة' : 'Page not found' %></h1>
    <p class="mt-2 text-sm text-slate-600 dark:text-slate-300"><%= lang==='ar' ? 'الرابط غير صحيح أو تم نقل الصفحة.' : 'The link is invalid or the page was moved.' %></p>
    <a href="/#top" class="mt-6 inline-flex rounded-2xl bg-cyan-500 px-5 py-3 text-sm font-semibold text-white hover:bg-cyan-600"><%= lang==='ar' ? 'العودة للرئيسية' : 'Back home' %></a>
  </div>
</section>

<%- include("layout-bottom") %>
