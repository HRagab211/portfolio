apiVersion: v1
kind: Service
metadata:
  labels:
    app: portfolio
  name: portfolio
spec:
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: portfolio
  type: NodePort
status:
  loadBalancer: {}
